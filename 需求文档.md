# 需求文档：人物登场字段（appeared）

## 0. 前置说明

项目：**暗耻三国志**（tkinter 桌面端《三国志 IX》风格回合制策略原型）。

**请先完整阅读项目根目录的 `README.md`**，尤其是：
- §0 术语表（全文用词以它为准）
- §3.3 人物（Character 类）
- §3.6 剧本加载器（三层人物加载）
- §8.3 ★ 永久约束（49 条，不可违反）
- §9 剧本编辑器设计
- §10 编辑会话设计

本文档只描述**需求与约束**，具体怎么改由你决定。README 里已有的约定（命名、日志、Command 模式、数据驱动弹窗等）请严格遵循。

---

## 1. 背景问题

当前剧本加载是三层：
1. `assets/characters.json` 加载静态数据
2. `scenarios/*.json` 覆盖动态字段（faction / node / location / role）
3. `_filter_by_year` 按出生年和死亡年**筛人**——未成年的、已死的，直接从 `World.characters` 里删掉

由此带来三个问题：

- **编辑剧本时看不到未登场人物**，无法编辑他们的五维、势力、归属等
- **`death_year` 参与筛选**，历史人物长寿化 / 穿越设定无法落地
- **登场判定散落在加载器里、不可编辑**，无法在编辑器里手动调整

---

## 2. 需求

### 2.1 核心目标

给人物增加**登场状态字段** `appeared`，让剧本编辑器能**查看和编辑未登场人物**，并能通过一键开关让未登场人物登场。

### 2.2 具体要求

1. **`Character` 增加 bool 字段 `appeared`**
   - 表示该人物**在当前剧本中是否已登场**
   - 属于剧本动态字段（和 faction / node / location / role 同类，由剧本填充，不写进基础数据）

2. **`World.characters` 收全量人物**
   - 190 剧本 = id 1–1000，共 1000 人
   - 加载器**不再按年份筛人**，未登场人物也进 World（这样才可编辑）

3. **剧本生成时预计算 `appeared`**
   - `tools/build_scenario_190.py` 改为**把所有人物都写进剧本**（不再只写 448 个有归属的）
   - 每条 character 记录都带 `appeared` 字段
   - 判定规则见 §2.3

4. **加载器不再按年份筛人**
   - `_filter_by_year` 的筛选逻辑去掉，改为只读 `appeared` 字段
   - 方法可以改名 / 改造，语义改为「应用登场状态」

5. **不再消费 `death_year`**
   - 剧本加载 / 登场判断**都不再看 `death_year`**
   - 字段本身保留（历史人物长寿化 / 穿越设定）

6. **人物面板加「登场」分组维度**
   - 分组值：「已登场」/「未登场」
   - 默认分组维持现状（不强制改成按登场先分组）

7. **人物面板右键加「设为登场 / 设为未登场」开关**
   - 纯开关，**不碰归属字段**（faction / node / location / role）
   - 走 Command（项目约定：改 World 只走 Command，UI 不得直接赋值）
   - 非编辑模式下置灰（沿用项目现有的编辑入口标识机制）

8. **人物情报窗口**
   - 未登场人物的窗口标题追加「（未登场）」
   - 其它显示（雷达图、关系区）不受影响

9. **兼容性**
   - 老剧本无 `appeared` 字段 → **默认 True**（保持现有行为）
   - `character_id_range` 字段**保留兼容**（老剧本仍可用）
   - 190 新剧本**不再写** `character_id_range`

---

## 3. 规则细节（已定稿）

### 3.1 登场判定规则（生成期一次算死，写入剧本）

| 情况 | appeared |
|---|---|
| 穿越人物（id ≥ 1001） | False |
| 已被分配到势力 | True |
| `birth_year` 缺失 | False |
| `year - birth_year >= 16` | True |
| 其余 | False |

注：`death_year` **不参与判定**。

### 3.2 未登场人物的归属字段

- `faction / node / location / role` 一律 `null`
- 「设为登场」后**仍是「在野」状态**，归属靠未来的完整人物编辑补
- 「设为未登场」时**保留已有归属字段**（可逆）

### 3.3 剧本输出格式

190 剧本的 `characters` 段：
- 写**全量 1049 人**（含穿越人物）
- 每人 5 个字段：`appeared / faction / node / location / role`
- 按 id 排序输出

### 3.4 穿越人物

- 全量写进剧本（进 `World.characters`）
- `appeared` 一律 False
- 可由用户在编辑器里手动「设为登场」放出来

---

## 4. 不要做的事

- ❌ 不要删 `Character.is_appeared(year)` 方法——保留（与 `appeared` 字段并存）
- ❌ 不要删 `death_year` 字段——保留，只是不消费
- ❌ 不要动 `assets/characters.json`（`location_name` / `affiliation` / `portrait` 三个垃圾字段本期不清理）
- ❌ 不要改 `Character.apply_override` 的现有逻辑（它用 `hasattr` 自动处理新字段）
- ❌ 不要给人物面板加表格列（只加**分组维度** + **右键开关**）
- ❌ 不要改 `World.characters_of` / `count_characters_by_faction` 的口径（未登场人物 faction=null，天然不计入）
- ❌ 不要给「设为登场」加弹窗——纯开关
- ❌ 不要引入新的第三方依赖

---

## 5. 必须遵守的项目约束（摘自 README §8.3）

1. **改 World 只走 Command**：UI 层不得直接赋值实体字段，一切经 `EditSession.execute()`。
2. **编辑入口统一标识**：右键项用现有机制标 `edit`，由框架统一在非编辑模式下置灰。**不要**在每个入口里复制模式判断。
3. **日志一律用 `%s` 惰性格式化**，禁止 f-string。
4. **`Pillow` 只用于头像**，其它地方不得引入。
5. **新增可编辑字段时**：写 / 读 / 显示三处必须同步（README §8.3 第 43 条）。

---

## 6. 边界与兼容

| 场景 | 期望行为 |
|---|---|
| 老剧本无 `appeared` 字段 | 全部默认登场，行为同改造前 |
| 老剧本同时有 `character_id_range` | 加载器仍按 id 白名单过滤（保留兼容） |
| 穿越人物（1001–1049） | 进 World，appeared=False，可手动设为登场 |
| 用户改 appeared 后回到 baseline | `is_dirty()` 自动判定为干净，标题 ` *` 消失 |
| 「设为登场」再「设为未登场」 | 归属字段保留，appeared 翻回 False |

---

## 7. 验收清单

### 7.1 命令验收

```bash
python tools/build_scenario_190.py   # 覆盖 scenarios/default.json
python -m pytest tests/ -v
python main.py
```

### 7.2 手动验收

| 验收项 | 期望 |
|---|---|
| 人物面板行数 | 1049 行（原 448） |
| 分组条出现「登场」 | 勾选后分成「已登场」/「未登场」两组 |
| 未登场人物右键 | 出现「设为登场」；非编辑模式下置灰 |
| 点「设为登场」 | 该人 appeared 变 True；窗口标题出现 ` *` |
| Ctrl+Z | 撤销后回到未登场；` *` 消失 |
| Ctrl+S 保存 | 剧本里该条 appeared 被写为 true |
| 未登场人物右键「人物情报」 | 窗口标题带「（未登场）」 |
| 雷达图 / 关系区 | 显示正常，不受 appeared 影响 |
| 老剧本（无 appeared）加载 | 全部默认登场，行为同改造前 |

### 7.3 已知的连带影响（需一并处理或确认）

- `tests/test_scenario_writer.py` 若断言了 Character 字段集合，可能需要更新（**改测试，不改代码**）
- `scenarios/default.json` 体积会增大（448 → 1049 条 characters）——预期行为
- 人物面板「在野」组会变得很长（约 600 人）——预期行为（用户明确要求可见未登场人物）

---

## 8. 完成后请提供

1. 改动的文件清单（相对路径）
2. `python tools/build_scenario_190.py` 的输出（势力数 / 人物数 / 登场数 / 据点数）
3. `python -m pytest tests/` 的结果
4. §7.2 手动验收清单的勾选情况

---

**范围：本次任务涉及 `game/core/`、`game/ui/panels/`、`game/ui/` 顶层、`tools/` 下的相关文件。具体改哪些、怎么改由你判断，但必须遵守 §4、§5 的约束。**