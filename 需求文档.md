# 需求文档（v2）：剧本编辑器（第一阶段 · 瘦身版）

> **本文档为 v2 更新版**。相对 v1 的变化：
> - §0 新增「已确定决策总览」（D1–D11）
> - §2 明确菜单栏形态
> - §3.3 郡治约束歧义已定
> - §6.1 明确 `_nodes_ref` 注入方案
> - §7.2 明确双重 baseline 口径
> - §10.6 补 `Field` NamedTuple 完整定义
> - §10.7 补 `MainWindow` 与面板的交互接口
> - §11.1 补全 Command / CompositeCommand / EditSession 接口签名
> - §12 补验收细节
> - **§14 新增「实施顺序」**
> - **§15 新增「常见坑」**

---

## 0. 术语与前提

### 0.0 已确定决策总览（不可推翻）

| # | 决策 | 说明 |
|---|---|---|
| **D1** | `Faction._nodes_ref` = **`world.nodes` dict 引用** | 不是快照，是引用。World 装载末尾统一注入。 |
| **D2** | **双重 baseline** | `raw`（加载时原始 dict）+ `baseline_snap`（加载后立即 serialize 的快照）。保存时 `output = deepcopy(raw) + diff(current, baseline_snap)`。 |
| **D3** | **郡治至多一个**，允许取消 | 三场景：①取消→直接；②重设→弹窗二选一→`CompositeCommand`；③无冲突→直接。 |
| **D4** | **diff 写全部字段**（含 `owner/troops/gold/food`） | 未改动不写；改动才写。 |
| **D5** | **`Field` 用 `NamedTuple`**，6 种 kind | `int / str / bool / choice / color / readonly` |
| **D6** | **菜单栏沿用现有 Menubutton 风格** | 在「游戏」左侧加「文件」「编辑」两个 Menubutton。**不改成原生 menubar**。 |
| **D7** | **`EditSession` 后置注入到面板** | `SidePanel.set_edit_session(session, on_edit)` 遍历 `panels` 赋 `panel.edit_session`。不改 `GenericListPanel.__init__` 签名。 |
| **D8** | **快捷键绑 `root`**，弹窗打开时通过 `MainWindow._modal_open` 标志屏蔽 | 不用 unbind。 |
| **D9** | **模式判断只在 `MainWindow`** | `self.editable = (APP_MODE == MODE_EDIT)`；其它模块读 `self.edit_session is not None`，**不直接读 `APP_MODE`**。 |
| **D10** | **弹窗数据驱动** | 骨架不出现 `if 据点 ... elif 势力 ...`。校验走 `Field` 或面板侧 hook。 |
| **D11** | **`to_dict` 写全字段** | `Character.to_dict` 含 `portrait`；`Node.to_dict` 从零写；`Faction.to_dict` **不写** `gold/food`。 |

### 0.1 两种模式

- **游戏模式**：本轮**不实现**，`APP_MODE = MODE_GAME` 时退回当前版本行为。
- **剧本编辑模式**：本轮实现。

### 0.2 方案 C 范围

- ✅ 据点**静态字段**编辑：`type / level / is_capital / troops / gold / food`
- ✅ 势力**静态字段**编辑：`name / color / prestige / stance`
- ❌ `owner` 编辑、级联弹窗、势力消亡、势力新建 / 删除（推迟到第二阶段）

### 0.3 本轮交付

模式切换、菜单栏、据点编辑、势力编辑、**通用 undo/redo 框架**、增量保存、`Faction.gold/food` 派生值化、**可扩展的架构骨架**。

---

## 1. 模式切换

### 1.1 常量

`game/config/constants.py` **末尾追加**：

```python
# ============================================================
# 应用模式
# ============================================================
MODE_EDIT = "edit"     # 剧本编辑模式
MODE_GAME = "game"     # 游戏模式（本轮不实现）
APP_MODE = MODE_EDIT   # 全局开关：编译期切换
```

### 1.2 编辑模式行为差异

- 启动**跳过选剧本**，直接加载 `DEFAULT_SCENARIO_PATH`。
- 顶部「进行」按钮、回合推进、日期时钟 **禁用（`state="disabled"`）**。
- 其余行为不变。

### 1.3 实现约束（D9）

- **模式判断只在一个地方**：`MainWindow.__init__` 里 `self.editable = (C.APP_MODE == C.MODE_EDIT)`。
- 其它模块**不直接读 `APP_MODE`**，而是读 `self.edit_session is not None`（能力开关）。
- 面板右键「编辑」项的 `enabled` 检查 `self.edit_session is not None`。

---

## 2. 菜单栏

### 2.1 形态（D6）

**沿用现有 TopBar 的 Menubutton 风格**，不改成原生 `root.config(menu=...)`。

`top_bar.py::_build_right_menus` 里的 `menu_specs` 顺序改为：

```python
menu_specs = [
    ("文件", self._build_file_menu),      # 新增
    ("编辑", self._build_edit_menu),      # 新增
    ("游戏", self._build_game_menu),
    ("势力", self._build_faction_menu),
    ("命令", self._build_order_menu),
    ("查看", self._build_view_menu),
    ("帮助", self._build_help_menu),
]
```

### 2.2 「文件」菜单

| 项 | 快捷键 | 行为 |
|---|---|---|
| 选择剧本 | — | 文件对话框（默认 `scenarios/`）；有未保存改动 → 先弹确认框（§8） |
| 保存 | Ctrl+S | 覆盖当前剧本文件；无改动时状态栏提示，不写文件 |
| 另存为 | Ctrl+Shift+S | 文件对话框；保存后**上下文切换到新文件** |
| 退出 | — | 同关窗拦截 |

### 2.3 「编辑」菜单

| 项 | 快捷键 | 行为 |
|---|---|---|
| 撤销 | Ctrl+Z | 见 §11 |
| 重做 | Ctrl+Shift+Z | 见 §11 |

无可用操作时**置灰**（`state="disabled"`）。

### 2.4 置灰机制

TopBar 新增方法：

```python
def set_edit_state(self, can_undo: bool, can_redo: bool):
    """编辑菜单撤销/重做的置灰状态。由 MainWindow 主动调用。"""
```

`MainWindow` 在以下时机调用 `self._sync_undo_redo_state()`：
- `EditSession.execute` 后
- `EditSession.undo` / `redo` 后
- 保存 / 另存为后
- 切换剧本后

### 2.5 「进行」按钮禁用

TopBar 新增方法：

```python
def set_game_mode(self, enabled: bool):
    """enabled=False 时禁用「进行」按钮。"""
```

`MainWindow._build_layout` 末尾按 `self.editable` 调用一次。

---

## 3. 据点编辑

### 3.1 入口

**据点面板**右键 →「编辑」。仅对单选有效（`len(ctx.selected_rows) == 1`）。

### 3.2 弹窗字段

| 字段 | 可编辑 | kind |
|---|---|---|
| `id` | ❌ | readonly |
| `name` | ❌ | readonly |
| `coords` | ❌ | readonly（显示 `(lon, lat)`） |
| `owner` | ❌ | readonly（显示势力名或"无主"，用 `display_fn`） |
| `type` | ✅ | choice：`城` / `关隘` / `渡口` |
| `level` | ✅ | int，范围 1–10 |
| `is_capital` | ✅ | bool |
| `troops` | ✅ | int，≥ 0 |
| `gold` | ✅ | int，≥ 0 |
| `food` | ✅ | int，≥ 0 |

### 3.3 `is_capital` 郡治约束（D3：A 解读）

**约束：一个郡（`id[:4]`）至多一个郡治，允许取消后暂时为空。**

三种触发场景：

| 场景 | 本县现 | 同郡现状 | 用户操作 | 行为 |
|---|---|---|---|---|
| 1 | 是郡治 | — | 取消勾选 | 直接执行，1 个 undo 单元 |
| 2 | 非郡治 | 已有郡治 X | 勾上 | **弹窗二选一** → 见下 |
| 3 | 非郡治 | 无郡治 | 勾上 | 直接执行，1 个 undo 单元 |

**场景 2 的二选一弹窗**：

```
「XX 县已是本郡郡治。
   将 XX 县改为非郡治，本县设为郡治？[是 / 否]」
```

- 用户选「是」：`CompositeCommand([NodeEditCommand(X, {is_capital: False}), NodeEditCommand(self, {is_capital: True})])`，**算 1 个 undo 单元**。
- 用户选「否」：整个编辑操作放弃，弹窗也关闭（不写入任何改动）。

> **注意**：`owner` 字段本轮只读，**不触发任何级联**。

### 3.4 确定 / 取消

- 「确定」→ 应用到内存，记为一个 undo 单元（或 CompositeCommand）。
- 「取消」/ Escape → 无副作用。

---

## 4. 势力编辑

### 4.1 入口

**势力面板**右键 →「编辑」。仅对单选有效。

### 4.2 弹窗字段

| 字段 | 可编辑 | kind |
|---|---|---|
| `id` | ❌ | readonly |
| `name` | ✅ | str |
| `color` | ✅ | color |
| `prestige` | ✅ | int，≥ 0 |
| `stance` | ✅ | int，-100 ~ 100 |
| `gold` | ❌ | readonly（派生值） |
| `food` | ❌ | readonly（派生值） |

- 君主**不可改**。
- `name` 变更不影响其它数据（无外键）。
- 「确定」→ 应用并记为一个 undo 单元。

> **势力改名后**，由于其它面板显示势力名，需要触发**全局面板刷新**（`SidePanel.on_panel_edit()` → `refresh_all()`）。

---

## 5. 推迟到第二阶段的功能

以下功能本轮**不实现**，但架构上必须预留扩展点（§10.8）：

- 据点 `owner` 编辑
- `owner` 变更的级联弹窗（人物 faction / node / location 联动）
- 势力消亡（据点归零自动删势力 + 名下全下野）
- 新建势力
- 删除势力
- 人物编辑

---

## 6. `Faction.gold` / `food` 派生值化

### 6.1 设计（D1：方案 A）

**`Faction._nodes_ref` 是 `world.nodes` dict 的引用（不是快照）。**

理由：`owner` 会随编辑变化，快照会导致求和结果错误。持有 dict 引用后，`world.nodes[nid].owner = x` 的改动会自动反映到 `faction.gold`。

#### `Faction` 改造

```python
class Faction:
    def __init__(self, fid, name, color="#888888",
                 prestige=0, stance=0):        # ★ 去掉 gold / food 形参
        self.id = fid
        self.name = name
        self.color = color
        self.prestige = int(prestige)
        self.stance = int(stance)
        self._nodes_ref = None                 # ★ World 注入

    @property
    def gold(self):
        """名下据点金钱求和。无主据点不计。"""
        if self._nodes_ref is None:
            return 0
        return sum(n.gold for n in self._nodes_ref.values()
                   if n.owner == self.id)

    @property
    def food(self):
        """名下据点军粮求和。"""
        if self._nodes_ref is None:
            return 0
        return sum(n.food for n in self._nodes_ref.values()
                   if n.owner == self.id)

    @classmethod
    def from_dict(cls, fid, d):
        """从剧本 dict 构造。不再读 gold / food。"""
        return cls(
            fid=fid,
            name=d.get("name", fid),
            color=d.get("color", "#888888"),
            prestige=d.get("prestige", 0),
            stance=d.get("stance", 0),
        )

    def to_dict(self):
        """序列化。不写 gold / food。"""
        return {
            "name": self.name,
            "color": self.color,
            "prestige": self.prestige,
            "stance": self.stance,
        }
```

#### `World` 注入时机

`World` 加方法：

```python
def bind_factions(self):
    """给每个 Faction 注入 nodes dict 引用（供 gold/food property 用）。
    由 ScenarioLoader.from_dict 末尾调用。
    """
    for f in self.factions.values():
        f._nodes_ref = self.nodes
```

`ScenarioLoader.from_dict` **末尾**：

```python
cls._apply_node_overrides(world, raw.get("nodes") or {})

world.bind_factions()   # ★ 新增

return world
```

`ScenarioLoader._build_faction` **改为**：

```python
@staticmethod
def _build_faction(fid, fdata):
    return Faction.from_dict(fid, fdata)   # ★ 简化
```

### 6.2 剧本 JSON

- `factions` 段**不再写** `gold / food`。
- `nodes` 段保持写 `gold / food`（据点自身数据）。

### 6.3 `tools/build_scenario_190.py`

`main()` 里 `out` 构造的 `factions` 段去掉 `"gold": 5000, "food": 20000` 两行。`FACTIONS` dict 本身不含这两个字段，**不用改**。

### 6.4 影响面

- `FactionRow.from_faction` 里 `f.gold / f.food` **自动走 property**，UI 不改。
- 势力面板显示的金 / 粮 = 名下据点求和。
- 现有 `default.json` 里遗留的 `factions.gold/food` 字段：加载时**忽略**（`Faction.from_dict` 不再读）。
- **`GameState.change_gold` 里的 `pf.gold = ...`** 现在会报错（property 无 setter）：
  - **本轮先不动**（编辑模式不跑回合）
  - 加注释 `# TODO(phase3): GameState.change_gold 需适配派生值`

---

## 7. 增量保存

### 7.1 格式与规则（D4）

```json
"nodes": {
  "130301": {
    "owner": "0521",
    "troops": 8000,
    "gold": 1000,
    "food": 20000,
    "type": "城",         // 仅当被改过才写
    "level": 3,           // 仅当被改过才写
    "is_capital": true    // 仅当被改过才写
  }
}
```

**规则**（**所有字段都按 diff 结果写**）：

- 无主且未改任何静态字段 → **不写**。
- 无主但静态字段被改过 → 只写改动过的静态字段。
- 有主且未改任何字段 → 保留 `raw` 原值（保证 round-trip）。
- 有主且改动过 → `owner/troops/gold/food` **+ 被改过的静态字段**。

> **核心口径**：保存文件 = `raw 原样 + 用户增量`。未改动字段**不出现**新的值，保留 raw 的原始状态。

### 7.2 Dirty 判定（D2：双重 baseline）

加载剧本时保留**两份快照**：

| 快照 | 内容 | 用途 |
|---|---|---|
| `raw` | `json.load(path)` 原始 dict | **写回时作为模板**（保留所有未改动字段的原始形态） |
| `baseline_snap` | 加载后立即 `ScenarioWriter.serialize(world)` | **diff 对比基准** |

**关键点**：

- baseline 与 undo 栈**解耦**（栈只有 5 步，不足以追踪全部 dirty）。
- **撤销回初始 → `is_dirty()` 自动 False**（因为 `serialize(world) == baseline_snap`）。
- diff 机制**必须通用**（见 §10.5）。

### 7.3 保存流程

`ScenarioWriter.save(world, path, raw, baseline_snap)` 步骤：

1. `current = serialize(world)`
2. `delta = diff(current, baseline_snap)` → `{section: {id: {field: (old, new)}}}`
3. `output = deepcopy(raw)`
4. 遍历 delta，对每个 `(section, id, field)`，用 `current[section][id][field]` 覆盖 `output[section][id][field]`
   - 若 `id` 不在 `output[section]` 里，先建空 dict
5. 写 JSON 到 `path`

保存成功后 `MainWindow` 执行：
- `edit_session.rebase()` — 更新 baseline
- `edit_session.clear()` — 清空 undo/redo 栈
- `_baseline_raw = self._load_raw_scenario(path)` — 更新 raw 模板

### 7.4 另存为

保存成功后：
- `self._scenario_path = new_path`（上下文切换）
- 更新 baseline
- 清空 undo/redo 栈
- 更新 `_baseline_raw`

---

## 8. 未保存拦截

有未保存改动时（`edit_session.is_dirty() == True`），以下场景弹「放弃改动 / 取消」：

1. **关闭窗口**（`WM_DELETE_WINDOW`）
2. **「选择剧本」切换**

弹窗用 `messagebox.askyesnocancel("未保存的改动", "有未保存的改动，是否放弃？")`：
- 返回 `None`（取消）→ 中止操作
- 返回 `False`（否）→ 中止操作
- 返回 `True`（是）→ 继续，不保存直接丢弃

> 「另存为」是保存动作，**不拦截**。

---

## 9. 涉及文件清单

### 9.1 新增（11 个）

```
game/core/edit_session.py          Command / CompositeCommand / EditSession
game/core/edit_commands.py         NodeEditCommand / FactionEditCommand
game/core/scenario_writer.py       serialize / diff / save

game/ui/dialogs/__init__.py        空文件
game/ui/dialogs/field_spec.py      Field NamedTuple
game/ui/dialogs/edit_dialog.py     通用数据驱动弹窗
game/ui/dialogs/node_fields.py     NODE_FIELDS
game/ui/dialogs/faction_fields.py  FACTION_FIELDS

tests/__init__.py                  空文件
tests/test_composite_command.py    §12.2 第 19 条验收
tests/test_scenario_writer.py      §12.2 第 20 条验收
```

### 9.2 修改（12 个）

| 文件 | 改动位置 | 摘要 |
|---|---|---|
| `game/config/constants.py` | 末尾追加 | 加 3 个常量（§1.1） |
| `game/core/faction.py` | `__init__` / 加 property / 加 `from_dict` `to_dict` | §6.1 |
| `game/core/world.py` | 加方法 | `bind_factions()` + 3 个 TODO 占位（§6.1 / §10.8） |
| `game/core/node.py` | 加方法 | `to_dict()`（§10.4） |
| `game/core/scenario.py` | `_build_faction` / `from_dict` 末尾 | §6.1 |
| `game/ui/main_window.py` | 见 §10.7 | 菜单动作、快捷键、session、拦截 |
| `game/ui/top_bar.py` | `_build_right_menus` / 加方法 | §2 |
| `game/ui/side_panel.py` | 加方法 | `set_edit_session` / `on_panel_edit`（§10.7） |
| `game/ui/panels/list/panel.py` | 加 1 个类属性 | `edit_session = None` |
| `game/ui/panels/node_panel.py` | `context_menu_items` / 加 `_edit` | §3 |
| `game/ui/panels/faction_panel.py` | `context_menu_items` / 加 `_edit` | §4 |
| `tools/build_scenario_190.py` | `main()` 里 `out` 构造 | §6.3 |

### 9.3 不碰

`map_canvas.py` / `map_controller.py` / `panels/list/*`（除 `panel.py` 一行类属性）/ `character_panel.py` / `troop_panel.py` / `settings_window.py` / `settings_manager.py` / `settings_schema.py` / `style.py` / `character_info_window.py` / `renderer.py` / `viewport.py` / `geo_data.py`。

**例外**：`GenericListPanel` 类体内加一行类属性 `edit_session = None`（供 D7 注入）。**不改 `__init__` 签名。**

---

## 10. 架构设计要求（硬约束）

> 本节是给实现者的**强制要求**，不是建议。违反任何一条，本阶段视为不通过。

### 10.1 分层与依赖方向

- 严守现有依赖方向：`main → ui → core/map → config`。**禁止反向**。
- 新增的编辑框架落在 **`core/` 层**，不依赖任何 UI。
- **UI 层（弹窗、面板、菜单）不得直接修改 World**，只能：
  1. 构造一个 `Command`
  2. 交给 `EditSession.execute()`
- 任何"直接赋值 World 字段"绕过 session 的代码 = **bug**。

### 10.2 Command 框架的通用性

- `Command` 基类**不得包含任何据点/势力专属字段**。
- **必须提供 `CompositeCommand`**（见 §11.1）。理由：第二阶段 owner 级联 = 「改据点 owner」+「改 N 个人物」+「可能删势力」= 一个大命令含多个子命令。本阶段虽只用于郡治互斥，但框架必须先立起来。
- Command 必须能保存**执行前的旧值**，`undo` 才能精确还原。

### 10.3 World 修改的统一入口

- World 实体（Faction / Character / Node）的字段修改，**统一经由 Command**。
- 若 World 提供了增删实体的方法（`add_faction` / `remove_faction`），这些方法**只能被 Command 调用**，不得被 UI 直接调用。
- 第二阶段要加的聚合查询（如 `count_nodes_by_owner`）放 World，**不放 Faction**，避免 Faction 反向依赖 World。

### 10.4 序列化对称性

- `to_dict` / `from_dict` 必须对**所有将来可能被编辑的字段**都已支持。
- `Character.to_dict` **必须含** `portrait` / `faction` / `node` / `location` / `role`（现已完整，不动）。
- `Node.to_dict` **从零写**，含 `owner / troops / gold / food / type / level / is_capital`。
- `Faction.to_dict` **不写** `gold / food`（派生值）。
- 理由：第二阶段加 owner 编辑时，序列化不该再动。

### 10.5 Dirty 判定的泛化

- baseline 对比必须是**通用机制**：快照整份 World，逐实体逐字段 diff。
- **禁止**为"据点"写一套 diff、为"人物"再写一套。
- diff 输出 = `{实体类型: {id: {字段: (旧值, 新值)}}}` 的统一结构。

### 10.6 UI 弹窗的数据驱动（D5）

#### `Field` NamedTuple

```python
from typing import NamedTuple, Optional, Callable, Any

class Field(NamedTuple):
    key: str                          # 实体属性名
    label: str                        # UI 显示名
    kind: str                         # "int"/"str"/"bool"/"choice"/"color"/"readonly"
    default: Any = None
    options: tuple = ()               # choice 用：[(value, label), ...]
    min: Optional[int] = None         # int 用
    max: Optional[int] = None
    editable: bool = True
    hint: str = ""                    # UI 下方提示
    display_fn: Optional[Callable] = None
        # readonly 用：fn(value, world) -> str
        # 例如 owner：把 fid 显示为势力名或"无主"
```

#### `EditDialog` 骨架

- 遍历 `fields` 渲染控件：
  - `readonly` → `Label`（值走 `display_fn`）
  - `int` → `Entry` + 范围校验
  - `bool` → `Checkbutton`
  - `choice` → `Combobox`
  - `color` → 色块 + `Entry`（复用 `settings_window._build_color` 的实现模式，**复制代码，不要 import**）
  - `str` → `Entry`
- 窗口居中：`center_on_parent(self, master, w, h)`，`update_idletasks` 后再居中。
- 模态：`grab_set()` + `wait_window`。`Escape` 关闭。
- **骨架中不得出现 `if 据点 ... elif 势力 ...` 这类分支**。

#### 字段表

**`NODE_FIELDS`**（`game/ui/dialogs/node_fields.py`）：

```python
NODE_FIELDS = (
    Field("id",         "编号", "readonly"),
    Field("name",       "县名", "readonly"),
    Field("coords",     "坐标", "readonly",
          display_fn=lambda v, w: f"({v[0]:.2f}, {v[1]:.2f})" if v else "—"),
    Field("owner",      "势力", "readonly",
          display_fn=lambda v, w: (w.faction(v).name if v and w and w.faction(v)
                                    else "无主")),
    Field("type",       "类型", "choice",
          options=(("城", "城"), ("关隘", "关隘"), ("渡口", "渡口"))),
    Field("level",      "规模", "int", min=1, max=10),
    Field("is_capital", "郡治", "bool"),
    Field("troops",     "兵力", "int", min=0),
    Field("gold",       "金钱", "int", min=0),
    Field("food",       "军粮", "int", min=0),
)
```

**`FACTION_FIELDS`**（`game/ui/dialogs/faction_fields.py`）：

```python
FACTION_FIELDS = (
    Field("id",       "编号",  "readonly"),
    Field("name",     "势力名", "str"),
    Field("color",    "颜色",  "color"),
    Field("prestige", "威望",  "int", min=0),
    Field("stance",   "关系",  "int", min=-100, max=100),
    Field("gold",     "金钱",  "readonly"),
    Field("food",     "军粮",  "readonly"),
)
```

#### `EditDialog.get_changed()`

返回 `(new_values, old_values)`，**只含真正变化的字段**：
- 比较方式：`bool` / `int` / `str` 直接 `!=`
- `coords` 是 tuple 不参与编辑
- `owner` 是 readonly 不进 changed
- 势力弹窗里 `gold/food` 是 readonly 不进 changed

### 10.7 模式切换与面板交互（D7 / D8 / D9）

#### `MainWindow` 侧

```python
# __init__
self.editable = (C.APP_MODE == C.MODE_EDIT)
self.edit_session = None
self._modal_open = False
self._scenario_path = str(C.DEFAULT_SCENARIO_PATH)

# _load_default_scenario 末尾
if self.editable:
    from game.core.scenario_writer import ScenarioWriter
    from game.core.edit_session import EditSession
    baseline_snap = ScenarioWriter.serialize(world)
    self.edit_session = EditSession(world, baseline_snap)
    self._baseline_raw = self._load_raw_scenario(path)
    self.side_panel.set_edit_session(
        self.edit_session, on_edit=self.on_edit_executed)
    self._sync_undo_redo_state()

def open_edit_dialog(self, dlg_factory):
    """打开模态弹窗，屏蔽全局快捷键。供面板调用。"""
    self._modal_open = True
    try:
        dlg = dlg_factory()
        self.wait_window(dlg)
        return dlg
    finally:
        self._modal_open = False

def on_edit_executed(self):
    """面板编辑执行后由 SidePanel 转发。"""
    self._sync_undo_redo_state()

def _sync_undo_redo_state(self):
    if self.edit_session is None:
        self.top_bar.set_edit_state(False, False)
    else:
        self.top_bar.set_edit_state(
            self.edit_session.can_undo(),
            self.edit_session.can_redo(),
        )
```

#### `SidePanel` 侧

```python
def set_edit_session(self, session, on_edit=None):
    """后置注入编辑会话。MainWindow 建好 session 后调用。"""
    self._edit_callback = on_edit
    for p in self.panels.values():
        p.edit_session = session

def on_panel_edit(self):
    """面板编辑执行后：刷新所有面板 + 转发给 MainWindow。"""
    self.refresh_all()
    cb = getattr(self, "_edit_callback", None)
    if callable(cb):
        cb()
```

#### `GenericListPanel` 侧

类体内加 1 行：

```python
edit_session = None    # ★ 由 SidePanel.set_edit_session 注入
```

#### 面板 `_edit` 里的模态弹窗

Panel 通过 `self.master` 向上找带 `open_edit_dialog` 的对象（`MainWindow`），或让 `SidePanel` 转发：

```python
def _open_dialog(self, dlg_factory):
    """向上找 MainWindow 的 open_edit_dialog。"""
    w = self.master
    while w is not None:
        hook = getattr(w, "open_edit_dialog", None)
        if callable(hook):
            return hook(dlg_factory)
        w = getattr(w, "master", None)
    # 兜底：直接开
    return dlg_factory()
```

`_edit` 方法流程（以据点为例）：

```python
def _edit(self, row):
    if self.edit_session is None:
        return
    world = getattr(self.game_state, "world", None)
    if world is None:
        return
    node = world.nodes.get(row.node_id)
    if node is None:
        return

    from game.ui.dialogs.edit_dialog import EditDialog
    from game.ui.dialogs.node_fields import NODE_FIELDS
    from game.core.edit_commands import NodeEditCommand
    from game.core.edit_session import CompositeCommand
    from tkinter import messagebox

    dlg = self._open_dialog(lambda: EditDialog(
        self, NODE_FIELDS, node, world=world, title="编辑据点"))
    if dlg is None or not dlg.ok:
        return

    new_values, old_values = dlg.get_changed()
    if not new_values:
        return

    # 郡治互斥（§3.3 场景 2）
    cmds = []
    if new_values.get("is_capital") is True:
        for other in world.nodes.values():
            if other.id == node.id or other.county_id != node.county_id:
                continue
            if not other.is_capital:
                continue
            ans = messagebox.askyesno(
                "郡治冲突",
                f"{other.name} 已是本郡郡治。\n"
                f"确定将其改为非郡治，本县设为郡治？",
            )
            if not ans:
                return
            cmds.append(NodeEditCommand(
                other.id, {"is_capital": True}, {"is_capital": False}))
            break

    cmds.append(NodeEditCommand(node.id, old_values, new_values))
    cmd = cmds[0] if len(cmds) == 1 else CompositeCommand(cmds, "设置郡治")
    self.edit_session.execute(cmd)
    self._notify_edit()

def _notify_edit(self):
    """向上找 SidePanel 的 on_panel_edit。"""
    w = self.master
    while w is not None:
        hook = getattr(w, "on_panel_edit", None)
        if callable(hook):
            hook()
            return
        w = getattr(w, "master", None)
    self.refresh()
```

### 10.8 预留但不实现的扩展点

下列内容本阶段**不实现**，但架构上必须**留出接入位置**（注释标记 `# TODO(phase2):` 即可，不写空壳代码）：

| 扩展点 | 预留位置 |
|---|---|
| owner 编辑 + 级联 | `NodeEditCommand` 的字段表加一行；级联逻辑走 `CompositeCommand` |
| 势力消亡 | `World.remove_faction_if_empty`；由 `CompositeCommand` 调用 |
| 新建 / 删除势力 | `World.add_faction` / `remove_faction`；对应 Command |
| 人物编辑 | 复用 §10.6 弹窗骨架；新增 `CharacterEditCommand` + `CHARACTER_FIELDS` |
| 多步撤销 + 分组 | `EditSession` 已是列表栈，天然支持 |

`World` 里三个占位：

```python
def add_faction(self, faction):
    """新增势力。只能被 Command 调用，UI 不得直接调。"""
    # TODO(phase2): 实现
    raise NotImplementedError("phase2")

def remove_faction(self, fid):
    """删除势力。只能被 Command 调用。"""
    # TODO(phase2): 实现
    raise NotImplementedError("phase2")

def remove_faction_if_empty(self, fid):
    """势力据点归零时删除。供 CompositeCommand 级联调用。"""
    # TODO(phase2): 实现
    raise NotImplementedError("phase2")
```

### 10.9 代码风格一致性

- 新增文件遵循现有命名 / 注释风格（中文注释，模块 docstring）。
- 面板改动沿用 `GenericListPanel` 的扩展点（`context_menu_items`），**不重写面板**。
- 不引入新第三方依赖。

---

## 11. 通用撤销 / 重做框架

### 11.1 接口（D-§11.1 完整签名）

#### `game/core/edit_session.py`

```python
"""通用编辑会话：Command 模式 + 深度限制 + dirty 联动。

本模块不感知具体业务。所有改 World 的操作必须封装成 Command，
经 EditSession.execute() 执行。
"""

class Command:
    """单步命令基类。"""
    def do(self, world):
        raise NotImplementedError
    def undo(self, world):
        raise NotImplementedError
    def label(self) -> str:
        return "操作"


class CompositeCommand(Command):
    """组合命令：do 顺序执行、undo 逆序执行。

    第二阶段级联的核心。本阶段用于郡治互斥。
    """
    def __init__(self, commands, label="复合操作"):
        self.commands = list(commands)
        self._label = label

    def do(self, world):
        for c in self.commands:
            c.do(world)

    def undo(self, world):
        for c in reversed(self.commands):
            c.undo(world)

    def label(self):
        return self._label


class EditSession:
    max_depth = 5

    def __init__(self, world, baseline_snapshot: dict):
        """baseline_snapshot 是加载后立即 serialize(world) 得到的快照 dict。"""
        self._world = world
        self._baseline = baseline_snapshot
        self._undo_stack = []   # list[Command]
        self._redo_stack = []   # list[Command]

    def execute(self, cmd):
        """do + 入 undo 栈 + 清空 redo + 深度裁剪。"""
        cmd.do(self._world)
        self._undo_stack.append(cmd)
        if len(self._undo_stack) > self.max_depth:
            self._undo_stack.pop(0)
        self._redo_stack.clear()

    def undo(self):
        """弹 undo + undo + 入 redo 栈。栈空直接返回。"""
        if not self._undo_stack:
            return
        cmd = self._undo_stack.pop()
        cmd.undo(self._world)
        self._redo_stack.append(cmd)

    def redo(self):
        """弹 redo + do + 入 undo 栈。栈空直接返回。"""
        if not self._redo_stack:
            return
        cmd = self._redo_stack.pop()
        cmd.do(self._world)
        self._undo_stack.append(cmd)

    def can_undo(self) -> bool:
        return bool(self._undo_stack)

    def can_redo(self) -> bool:
        return bool(self._redo_stack)

    def is_dirty(self) -> bool:
        """current = serialize(world); return bool(diff(current, baseline))"""
        from game.core.scenario_writer import ScenarioWriter
        current = ScenarioWriter.serialize(self._world)
        return bool(ScenarioWriter.diff(current, self._baseline))

    def clear(self):
        """清空 undo/redo 栈。保存 / 另存为后调用。"""
        self._undo_stack.clear()
        self._redo_stack.clear()

    def rebase(self):
        """重设 baseline = serialize(world)。保存 / 另存为后调用。"""
        from game.core.scenario_writer import ScenarioWriter
        self._baseline = ScenarioWriter.serialize(self._world)
```

#### `game/core/edit_commands.py`

```python
"""具体命令：据点编辑、势力编辑。

约定：old_values / new_values 都是 {field: value} dict，只含真正变化的字段。
"""

class NodeEditCommand(Command):
    def __init__(self, node_id, old_values: dict, new_values: dict):
        self.node_id = node_id
        self.old_values = dict(old_values)
        self.new_values = dict(new_values)

    def do(self, world):
        n = world.nodes[self.node_id]
        for k, v in self.new_values.items():
            setattr(n, k, v)

    def undo(self, world):
        n = world.nodes[self.node_id]
        for k, v in self.old_values.items():
            setattr(n, k, v)

    def label(self):
        return f"编辑据点 {self.node_id}"


class FactionEditCommand(Command):
    # 同构，操作 world.factions
    ...

# TODO(phase2): NodeEditCommand 支持 owner 字段 + 级联
# TODO(phase2): 新增 FactionCreateCommand / FactionDeleteCommand
# TODO(phase2): 新增 CharacterEditCommand
```

#### `game/core/scenario_writer.py`

```python
"""World ↔ JSON 序列化 + diff + 增量保存。"""

class ScenarioWriter:
    @staticmethod
    def serialize(world) -> dict:
        """World → 完整 dict。
        含元字段：version / id / name / desc / start / player_faction / character_id_range
        factions 段：不写 gold/food
        characters 段：全字段（含 portrait / faction / node / location / role）
        nodes 段：全字段（owner / troops / gold / food / type / level / is_capital）
        """

    @staticmethod
    def diff(current: dict, baseline: dict) -> dict:
        """逐实体逐字段对比。
        返回 {section: {id: {field: (old, new)}}}
        只返回变化的字段。
        section ∈ {"factions", "characters", "nodes"}。
        元字段不参与 diff。
        """

    @staticmethod
    def save(world, path, raw: dict, baseline_snap: dict):
        """增量保存（§7.3 五步）。"""
```

### 11.2 命令粒度

| 操作 | undo 单元数 |
|---|---|
| 一次据点编辑（无郡治冲突） | 1 |
| 一次据点编辑（触发郡治互斥，用户选"是"） | 1（CompositeCommand） |
| 一次势力编辑 | 1 |
| （未来）一次 owner 编辑 + 级联 | 1（CompositeCommand） |
| （未来）新建 / 删除势力 | 1 |

### 11.3 快捷键与菜单

- `Ctrl+Z` 撤销 / `Ctrl+Shift+Z` 重做
- 「编辑 → 撤销 / 重做」
- 无可用操作时置灰
- 弹窗打开时（`_modal_open == True`），快捷键**直接 return**，不执行

### 11.4 生命周期

- 保存 / 另存为成功 → `rebase()` + `clear()`
- 切换剧本 → 重建 session（栈清空）
- 撤销到初始 → `is_dirty()` 自动 False

---

## 12. 验收点

### 12.1 功能验收

| # | 验收点 | 预期行为 |
|---|---|---|
| 1 | 启动 → 编辑模式 | `APP_MODE=edit`，加载默认剧本，顶部「进行」灰化 |
| 2 | Ctrl+S 无改动 | 状态栏提示「无改动，未保存」，不写文件 |
| 3 | Ctrl+S 有改动 | 覆盖当前剧本文件，`rebase + clear` |
| 4 | 另存为 | 弹出文件对话框；保存后 `_scenario_path` 切换；再 Ctrl+S 写新文件 |
| 5 | 有改动关闭窗口 | 弹「放弃改动 / 取消」，取消则不关 |
| 6 | 据点右键 → 编辑 | 弹窗含 10 字段；`owner` 只读；改 type/level/is_capital/troops/gold/food |
| 7 | 勾 `is_capital=True` 同郡冲突 | 弹二选一；选「是」→ 旧郡治改非 + 本县郡治；选「否」→ 整体放弃 |
| 8 | 势力右键 → 编辑 | 弹窗 7 字段；`gold/food` 只读展示派生值 |
| 9 | 改任一据点 gold → 势力面板刷新 | 势力面板 gold = 名下据点求和，实时变化 |
| 10 | round-trip | 保存的 JSON 能被 `ScenarioLoader.load` 重新加载且状态一致 |
| 11 | 未改动字段不出现 | 保存文件里未编辑的据点字段保持 raw 原值；无主未改的据点不出现 |
| 12 | `factions` 段不含 `gold/food` | 新保存的文件里 factions 只有 name/color/prestige/stance |
| 13 | 加载现有 default.json | 里面遗留的 `factions.gold/food` 被忽略，不报错 |
| 14 | 据点编辑后 Ctrl+Z | 完全回滚（含郡治联动：用 CompositeCommand 时一并回滚） |
| 15 | 撤销后 Ctrl+Shift+Z | 恢复 |
| 16 | 连续 6 次编辑后 | 只能撤销最近 5 次（`max_depth=5`） |
| 17 | 撤销到初始状态 | 关闭窗口**不**弹确认框（`is_dirty()=False`） |
| 18 | 保存后 | 撤销/重做菜单项置灰 |

### 12.2 架构验收

| # | 验收点 | 检查方法 |
|---|---|---|
| 19 | 全局搜索无 UI 直接赋值 | 搜 `world.nodes[...].X = `、`node.gold = ` 等，应只在 `edit_commands.py` 里出现 |
| 20 | `CompositeCommand` 单测 | `pytest tests/test_composite_command.py` 通过 |
| 21 | 序列化 round-trip | `pytest tests/test_scenario_writer.py` 通过 |
| 22 | 弹窗骨架无实体分支 | 搜 `edit_dialog.py` 无 `if.*据点` / `elif.*势力` |
| 23 | `COLUMNS = COLUMNS` 类属性绑定 | `faction_panel` / `node_panel` 都有（已有，不动） |

### 12.3 测试文件规格

**`tests/test_composite_command.py`**：

```python
"""§12.2 第 19 条验收。"""

def test_composite_do_undo():
    # 构造一个最小 World（或 mock，含一个 nodes dict）
    # 定义两个简单 Command：各自改一个字段
    # composite = CompositeCommand([c1, c2])
    # composite.do(world) → 两个字段都变
    # composite.undo(world) → 两个字段都还原
```

**`tests/test_scenario_writer.py`**：

```python
"""§12.2 第 20 条验收。"""

def test_roundtrip():
    # 加载 assets/characters.json + map.geojson（或用最小 mock world）
    # serialized = ScenarioWriter.serialize(world)
    # 检查 serialized 含 characters.faction/node/location、nodes.owner 等
    # 用 ScenarioLoader.from_dict(serialized, geo_data) 重建
    # 对比关键实体的关键字段一致
```

---

## 13. 明确不做（本阶段）

- 游戏模式逻辑（回合、出兵、内政）
- 据点 `owner` 编辑 + 全部级联逻辑
- 势力消亡、势力新建 / 删除
- 人物面板 / 人物情报窗口的编辑
- 部队面板
- 据点 `id/name/coords/boundary` 编辑
- 多选批量编辑
- 面板列配置变动
- 弹窗字段的滚动（字段 ≤ 10 个，无需滚动）

---

## 14. 实施顺序（建议）

**每个阶段完成后单独提交**，便于回滚。

### 阶段 1：核心层（不接 UI）

1. `game/config/constants.py` — 加 3 个常量
2. `game/core/edit_session.py` — 新增
3. `game/core/scenario_writer.py` — 新增
4. `game/core/faction.py` — 改 property + `from_dict` / `to_dict`
5. `game/core/node.py` — 加 `to_dict`
6. `game/core/world.py` — 加 `bind_factions` + 3 个 TODO 占位
7. `game/core/scenario.py` — 改 `_build_faction` + 末尾加 `bind_factions`
8. `game/core/edit_commands.py` — 新增
9. `tests/test_composite_command.py` + `tests/test_scenario_writer.py` — 新增
10. **跑测试**：`pytest tests/`

### 阶段 2：弹窗（不接面板）

11. `game/ui/dialogs/__init__.py` — 空
12. `game/ui/dialogs/field_spec.py` — 新增
13. `game/ui/dialogs/edit_dialog.py` — 新增
14. `game/ui/dialogs/node_fields.py` — 新增
15. `game/ui/dialogs/faction_fields.py` — 新增

### 阶段 3：接入面板

16. `game/ui/panels/list/panel.py` — 加 `edit_session = None` 类属性（**一行**）
17. `game/ui/side_panel.py` — 加 `set_edit_session` + `on_panel_edit`
18. `game/ui/panels/node_panel.py` — 加右键项 + `_edit` + `_open_dialog` + `_notify_edit`
19. `game/ui/panels/faction_panel.py` — 加右键项 + `_edit`

### 阶段 4：接入主窗口

20. `game/ui/top_bar.py` — 加两个菜单 + `set_edit_state` + `set_game_mode`
21. `game/ui/main_window.py` — 全部改动（§10.7）

### 阶段 5：收尾

22. `tools/build_scenario_190.py` — 去掉 gold/food 输出
23. **跑验收**：§12.1 全部 18 条 + §12.2 全部 5 条
24. 更新 `README.md`（参照 §9.16 格式写 §9.17 变更日志）

---

## 15. 常见坑（务必注意）

1. **`Faction.gold/food` 是 property**：不能再 `faction.gold = x` 赋值。`GameState.change_gold` 里有 `pf.gold = ...`，**本阶段先不动**（编辑模式不跑回合），但加注释 `# TODO(phase3): GameState.change_gold 需适配派生值`。

2. **`baseline_snap` 必须在 `bind_factions()` 之后序列化**：否则 `faction.gold` 会是 0。**顺序**：`bind_factions` → `serialize` → `EditSession(world, baseline_snap)`。

3. **`raw` 必须保留用于增量写回**：不能省。`save(world, path, raw, baseline_snap)` 三个参数都要。

4. **`Ctrl+Shift+S` / `Ctrl+Shift+Z` 在 Tk 里 keysym 是大写 `S` / `Z`**：
   - 正确：`r.bind("<Control-Shift-S>", ...)`、`r.bind("<Control-Shift-Z>", ...)`
   - 错误：`r.bind("<Control-Shift-s>", ...)`（小写不触发）

5. **`Menu` 保引用**：TopBar 已有 `self._menus` 列表；`_make_menu_button` 已做了 `append`。新增菜单不用管。

6. **`EditDialog.get_changed` 的比较**：
   - `bool` / `int` / `str` 直接 `!=`
   - `coords` 是 tuple 不参与编辑
   - `owner` 是 readonly 不进 changed
   - `Faction.gold/food` 是 property，readonly 不进 changed

7. **`CompositeCommand` 的 `label()`**：郡治互斥场景显示「设置郡治」，其它场景直接 `NodeEditCommand.label()`。

8. **`SidePanel.on_panel_edit` 递归风险**：`refresh_all` → `panel.refresh` → 不触发 `_edit`，安全。**不要**在 `_edit` 里调 `on_panel_edit` 后又调 `self.refresh()`（会重复刷新）。

9. **面板向上找 `MainWindow`**：
   - `panel.master` 是 `Notebook`
   - `Notebook.master` 是 `SidePanel`
   - `SidePanel.master` 是 `PanedWindow`
   - `PanedWindow.master` 是 `root`（Tk 实例，**不是 `MainWindow`**）
   
   **`MainWindow` 不是 widget**，是普通 Python 对象。
   
   所以 `panel` **不能通过 `master` 链找到 `MainWindow`**。
   
   **正确做法**：`SidePanel` 存一个 `self._edit_callback`（由 `MainWindow` 传入），`panel` 通过 `self.master` 找到 `SidePanel`（`Notebook.master` 就是 `SidePanel`），再调 `side_panel.on_panel_edit()`。

10. **弹窗打开时的快捷键屏蔽**：
    - `MainWindow.open_edit_dialog(dlg_factory)` 里设 `self._modal_open = True`，`finally` 里清零
    - `_on_undo` / `_on_redo` / `_on_save_scenario` 等入口判断 `if self._modal_open: return`
    - **不要**用 `unbind`，太脆弱

11. **`_load_default_scenario` 里 `self._scenario_path` 的初始化**：
    - `__init__` 里先设 `self._scenario_path = str(C.DEFAULT_SCENARIO_PATH)`
    - `_load_default_scenario` 成功后再确认

12. **`ScenarioWriter.serialize` 的元字段来源**：
    - `version` / `id` / `name` / `desc` / `start` / `player_faction` / `character_id_range`
    - 从 `world` 对象读（`world.version` / `world.id` / ... / `world.player_faction_id`）
    - **`character_id_range` 从哪来？** 建议存 `world.character_id_range = raw.get("character_id_range")`，`ScenarioLoader` 里加一行

---

**文档结束。**

**给 VSCode Claude 的提示**：请严格按 §14 顺序实施，每完成一个阶段运行一次测试。所有修改文件的具体改动点见 §9.2 和 §10.7。常见坑见 §15。