# 需求文档：Panel 通用框架 + 地图交互

## 一、背景

当前右侧 4 个 panel（据点 / 人物 / 势力 / 部队）各自实现了一套列表、排序、分组、右键逻辑，重复代码多。同时地图交互仅限于 hover 状态栏提示，缺少点选、反向定位、右键菜单、视觉高亮等常用功能。

本需求分两大部分：

1. **Panel 通用框架重构**（全量迁移 4 个 panel）
2. **地图交互增强**（点选 / hover 高亮 / 右键菜单 / 双向定位）

**注意：本期只做 UI 层与交互层，不改任何数据。**

---

## 二、需求 1：Panel 通用框架重构（全量）

### 2.1 目标

4 个 panel 共享一套基础设施。**每个具体 panel 只写配置**（列定义、分组维度、右键菜单项、数据源、定位逻辑），通用能力全部由框架提供。

### 2.2 覆盖范围

**全量迁移**：

- 据点 panel（现 `panels/node/`）
- 人物 panel（现 `panels/character/`）
- 势力 panel（现 `panels/faction_panel.py`）
- 部队 panel（现 `panels/troop_panel.py`，空壳，仅接入框架）

### 2.3 通用能力清单（框架提供）

| 能力 | 说明 |
|---|---|
| **列表** | Treeview + 列定义（key / 标题 / 宽度 / 对齐 / 取值函数） |
| **排序** | 点列头切换升/降序，空值永远排最后 |
| **分组** | 多维度、正交、点选顺序 = 嵌套顺序；支持"固定分组"模式（见 2.4 势力 panel） |
| **搜索** | 见需求 2 |
| **定位到地图** | 右键菜单项；框架暴露钩子，具体 panel 决定如何定位 |
| **反向定位（地图 → 列表）** | 框架暴露方法 `scroll_to_*`，供 MapController / MainWindow 调用 |
| **右键菜单** | 从配置构建；支持分隔符、子菜单、动态启用/禁用 |
| **多选** | Ctrl+点击 / Shift+点击 / Ctrl+A；右键菜单作用于所有选中行 |
| **列定义驱动** | 新增列只改配置，不改框架代码 |

### 2.4 各 panel 差异点（框架必须容纳）

| Panel | 特殊点 |
|---|---|
| 据点 | 默认分组 `州 > 郡`；右键「定位到地图」；人物 / 主官列占位 |
| 人物 | 默认分组 `势力`；**玩家势力置顶**；右键「复制编号」 |
| 势力 | **固定分组**（玩家 / 盟友 / 敌对 / 中立），不是"多维度可选"；**势力色块**（带黑边，行高自适应）；右键待定 |
| 部队 | 空壳，先接入框架，数据留白 |

**关键设计点**：

- 通用分组模式 = "维度可选，点选顺序 = 嵌套顺序"
- 势力 panel 的"固定分组"是另一种模式 → 框架需要支持**自定义分组回调**（返回分组树），覆盖默认的维度选择逻辑
- 势力色块 = **列渲染扩展点**（某列可以自定义"在文本前加图片"）→ 框架需要支持**自定义列渲染**

### 2.5 接口契约（建议，非强制）

具体 panel 继承框架基类，提供以下配置：

```
COLUMNS         : tuple[Column, ...]         # 列定义
NAME_COLUMN     : Column                     # #0 列（通常是名称列）
GROUP_DIMS      : dict[key, (显示名, 取值函数)]  # 可选分组维度
DEFAULT_GROUP   : list[key]                  # 默认选中的分组
PRIORITY_NAME   : str | None                 # 置顶组名（人物 panel 用）

fetch_rows()                  -> list[Row]          # 数据源
context_menu_items()          -> list[MenuItem]     # 右键菜单配置
locate_on_map(row)            -> None               # 定位到地图
scroll_to_node(node_id)       -> None               # 反向定位响应（据点 panel 用）
```

框架负责：

- 构建 UI（搜索框 + 分组条 + Treeview + 滚动条）
- 排序 / 分组 / 搜索的调度
- 右键菜单的弹出、多选处理
- 选中状态管理
- 与 MapController 的对接（回调注册）

### 2.6 包结构

**由实现者决定**。只需满足：通用代码集中在一个地方，具体 panel 只写配置。

参考方案（仅供参考，可推翻）：

```
panels/
├── list/                    # 通用框架
│   ├── panel.py             GenericListPanel
│   ├── model.py             ListRow 基类
│   ├── columns.py           Column 定义
│   ├── sorting.py
│   ├── grouping.py
│   ├── group_bar.py
│   ├── search_bar.py
│   ├── context_menu.py
│   └── locator.py
├── node.py                  具体 panel（瘦身为配置）
├── character.py
├── faction.py
└── troop.py
```

---

## 三、需求 2：搜索功能

### 3.1 交互（VSCode 风格）

- 搜索框位于每个 panel 顶部（分组条上方）
- **实时**：每输入一个字符就刷新
- **模糊**：包含匹配
- **多词 AND**：空格分隔，全部匹配才显示
- **过滤隐藏**：不匹配的行隐藏；**保留分组结构**（组头还在，只是组内过滤）
- **清空按钮**：搜索框右侧一个"×"，点击清空
- **高亮**：匹配部分可以不高亮（第一版不做）

### 3.2 本期范围

- 只搜**当前 panel 的可见字段**（`COLUMNS` 里定义的列 + `#0` 列）
- 大小写不敏感

### 3.3 预留接口

- **跨字段搜索**：搜索框加前缀语法（如 `field:value`）→ 本期只留接口
- **正则表达式**：搜索框加开关或前缀 `/.../`→ 本期只留接口
- **模糊搜索**：更高级的模糊（拼音、缩写）→ 本期只留接口

---

## 四、需求 3：地图交互

### 4.1 左键点选

- **粒度**：只点选**县**（Node），不能点州/郡
- **单选**：地图上任何时刻最多一个县被选中
- **视觉**：选中县描边加粗（如 2px 亮青色）
- **取消**：再点同一县 / 点空白 / 海上 / 山上 → 取消选中
- **重要**：**点选不触发任何逻辑代码**（不滚动列表、不弹窗、不查 World）
- 点选后，右键菜单针对的是**被点选的县**（而非鼠标点的那个县，虽然通常一样）

### 4.2 hover 视觉互动

5 个开关，全部放在**设置窗口「操作」Tab**下，用户可独立开关：

| 开关 | 效果 | 建议默认 |
|---|---|---|
| `highlight_hover_border` | 该县描边加粗变亮（1px → 2px 亮色） | 开 |
| `highlight_hover_fill` | 该县填充色变亮（蒙一层白） | 开 |
| `highlight_hover_faction_all` | 该县所属势力的**所有县**同时高亮 | 关 |
| `highlight_hover_tooltip` | 弹 tooltip（浮窗）：县名 + 势力 + 驻军 | 开 |
| `highlight_hover_region` | hover 州/郡边界时高亮整片区域 | 关 |

**开关作用域**：全局，不分 panel。

### 4.3 地图右键菜单

在县上右键 → 弹出菜单，**结构参照据点 panel 的右键菜单**，但去掉列表专属项：

```
据点情报
人物情报
势力情报
─────
定位到列表 →
    据点
    人物
    势力
    部队
```

**说明**：

- 去掉"定位到地图"（在地图上右键是冗余的）
- 去掉"全部展开 / 全部折叠"（列表专属）
- 第一期**只实现"定位到列表 → 据点"**，其余 3 项**置灰占位**
- 在空白 / 海上 / 山上右键 → **弹轻量菜单**，只含：

```
复位视图
放大
缩小
```

---

## 五、需求 4：双向定位

### 5.1 地图 → 列表（反向定位）

**触发方式**：只能通过**右键菜单**（点选不触发）。

**行为**：

- 菜单项：`定位到列表 → 据点 / 人物 / 势力 / 部队`
- 第一期只实现「据点」
- 执行时：
  1. **自动切到目标 Tab**（如当前是人物 Tab，切到据点 Tab）
  2. 目标 panel 滚动到对应行
  3. 选中该行
  4. 若行在折叠的组内 → 展开所在组

**死循环防护**：反向定位时**不**触发"列表 → 地图"的逻辑，用 `_syncing` 标志位防递归。

### 5.2 列表 → 地图

**触发方式**：右键菜单「定位到地图」。

**多选时**：

- 菜单作用于**所有选中行**（如"据点情报"显示 10 个据点的情报）
- **但「定位到地图」例外**：只定位到**右键那一行**
- 单选时就是选中行

**行为**：调用 `MapController.fit_to_node(node_id)`，地图视图飞到该县。

---

## 六、本期明确不做的

| 项 | 说明 |
|---|---|
| **修改字段** | 任何数据修改功能都不做 |
| **撤销 / 重做** | 不做 |
| **数据写回文件** | 不做 |
| **地图多选 / 框选** | 地图上只支持单选 |
| **拼音 / 缩写搜索** | 只留接口 |
| **正则搜索实现** | 只留接口 |
| **跨字段搜索实现** | 只留接口 |
| **反向定位的其他 3 项**（人物 / 势力 / 部队） | 只做据点，其余占位 |
| **tooltip 自定义样式** | 用系统默认 |
| **面板状态持久化**（列宽 / 分组 / 排序） | 不做 |
| **列拖拽排序 / 显隐** | 不做 |

---

## 七、附录：现有实现参考

| 现有文件 | 用途 |
|---|---|
| `game/ui/panels/node/` | 据点 panel 现有实现（含完整的排序 / 分组 / 右键） |
| `game/ui/panels/character/` | 人物 panel 现有实现（含 `priority_name` 置顶） |
| `game/ui/panels/faction_panel.py` | 势力 panel 现有实现（含色块） |
| `game/ui/panels/troop_panel.py` | 部队 panel 空壳 |
| `game/ui/map_controller.py` | 地图中介，提供 `fit_to_node` |
| `game/ui/map_canvas.py` | 地图画布，hover 回调传 dict |
| `game/ui/main_window.py` | 装配工，`_on_location_change` 拼装状态栏 |
| `game/config/settings_schema.py` | 设置窗口 Schema（加 5 个 hover 开关） |
| `game/map/geo_data.py` | `find_location_detail` 提供 hover 数据 |

**已完成的相关功能（本轮不改动，但框架迁移时要保留）**：

- 势力面板色块（带黑边，行高自适应）
- 人物面板玩家势力置顶
- hover 显示 `州 · 郡 · 县 · 势力`
- 染色层不吃 LOD

---

## 八、给实现者的提示

1. **抽象优先**：先把框架搭好，再迁 panel。不要 4 个 panel 分别改。
2. **差异点用扩展点解决**：势力 panel 的"固定分组"和"色块"不是特例，要设计成框架的扩展点。
3. **接口契约要稳定**：具体 panel 只写配置，通用能力不能"漏"到具体 panel 里。
4. **视觉一致性**：4 个 panel 的搜索框、分组条、右键菜单外观保持一致。
5. **地图交互与面板解耦**：MapCanvas 只输出事件（点选 / 右键 / hover），由 MainWindow 分发到 panel 或 status_bar。
6. **所有新增 UI 交互都要可测试**：右键菜单的每一项、搜索的每个模式、定位的每个方向，都要能在无鼠标的情况下手动触发（方便调试）。