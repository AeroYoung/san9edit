# 需求文档：人物移动到据点

## 0. 前置说明

下游实现前必须读 README 的以下章节：

| 章节 | 用途 |
|---|---|
| §0 术语表 | 「所属（node）vs 所在（location）」、势力 id = 君主 id、「无主」定义 |
| §3.2 Faction | 势力 id 与君主关系 |
| §3.3 Character | node / location / faction / appeared 四字段语义 |
| §3.5 World 查询与聚合 | 移动后各面板计数如何自动反映 |
| §5.22 panels/list/ 框架 | 复用列表框架（排序 / 搜索 / 单选 / 列解析） |
| §5.23 node_panel / character_panel | 据点行模型、人物右键入口 |
| §8.3 第 36 / 40 条 | Command 唯一入口、编辑入口统一标识 |
| §9.6 共用编辑流程 | 守卫 / 弹窗 / get_changed 的既有模式 |
| §10.2 CharacterEditCommand | old/new 只含真正变化的字段 |

---

## 1. 背景问题

| 项 | 现状 |
|---|---|
| 人物归属调整 | 人物面板只能开关 `appeared`；`node` / `location` / `faction` 无编辑入口 |
| 编制变更 | 想让某人改属某据点，只能手工改剧本 JSON |
| 势力跟随 | 人物 `faction` 与 `node.owner` 的一致性全靠剧本生成期保证，编辑器无联动 |
| 风险 | 君主改属会导致势力 id 与 `faction` 脱钩；未登场人物改属后的登场状态无人询问 |

---

## 2. 需求目标

| 编号 | 目标 |
|---|---|
| G1 | 人物面板右键新增「移动到据点」，弹窗内单选一个据点 |
| G2 | 弹窗内嵌据点列表（复用据点面板能力），强制单选、禁用右键、不分组 |
| G3 | 弹窗内有实时信息块，随选中据点预估变化 |
| G4 | 选定后二次确认，展示变更明细 |
| G5 | `node` / `location` 改为目标据点；`faction` 自动跟随目标据点 owner（无主则下野） |
| G6 | 君主不能移到无主 / 他势力据点，阻断并提示 |
| G7 | 未登场人物移到有主据点时，询问是否同时设为登场（默认「是」） |
| G8 | 全部变更走 Command，`old/new` 只含真正变化的字段 |

---

## 3. 规则细节

### 3.1 入口

| 项 | 值 |
|---|---|
| 位置 | 人物面板右键菜单 |
| 文案 | `移动到据点` |
| 标识 | `edit=True`（非编辑模式由 `build_menu(edit_enabled=)` 统一置灰） |
| 生效范围 | 沿用框架现有右键规则：右键行不在选中集时单选它，否则对整个选中集生效 |
| 无选中数据行时 | 不弹菜单（与现有右键行为一致） |

### 3.2 弹窗结构

| 区域 | 内容 |
|---|---|
| 标题 | `移动到据点` |
| 上部 | 据点列表（行为等价于据点面板，按 §3.3 裁剪） |
| 下部 | 信息块（随选中行实时更新，见 §3.4） |
| 底部 | `确定` / `取消` 按钮 |
| 窗口 | `Toplevel`，模态（`grab_set`），相对主窗口居中 |
| Esc | 等同取消 |

### 3.3 据点列表裁剪

| 能力 | 处理 |
|---|---|
| 多选 | 强制单选（`selectmode="browse"`，禁用 Ctrl+A） |
| 右键菜单 | 禁用 |
| 分组 | 固定为不分组（无分组条） |
| 搜索 | 保留 |
| 列排序 | 保留 |
| 滚动 | 保留 |
| 列配置 | 读取 `PANEL_COLUMNS["node"]`（与据点面板共享用户配置） |

### 3.4 信息块内容

未选中据点时显示：`请选择一个据点`。

选中据点后显示（每项一行）：

| 行 | 内容 |
|---|---|
| 目标据点 | 州 · 郡 · 县 · 类型 · 规模 · 当前势力（无主则显示「无主」） |
| 选中人数 | 本次操作涉及的人数（不含被阻断者） |
| 会改变据点 | `node` 将变更的人数 |
| 仅换据点、势力不变 | 目标 owner == 人物当前 faction 的人数（**必须单独一行**） |
| 会变更势力 | 目标 owner ≠ 人物当前 faction 且目标有主的人数 |
| 会下野 | 目标无主、非君主、当前 faction 非空的人数 |
| 会设为登场 | 未登场且目标有主的人数（无论用户最终选「是」或「否」，此项按「是」的假设预估，需在文案上标注「将询问」） |
| 被阻断 | 君主且目标无主 / 他势力 → 列出姓名（≤ 3 个）或计数 |

### 3.5 目标据点分类

| 分类 | 判定 |
|---|---|
| 有主 | `node.owner` 非空 且 `world.factions` 存在该 id |
| 无主 | `node.owner` 为空，或 `node.owner` 存在但 `world.factions` 无对应（脏数据） |

### 3.6 单人变更规则

对每个选中人物逐条判定：

| # | 条件 | node | location | faction | appeared |
|---|---|---|---|---|---|
| 1 | 君主 且 目标无主 | 阻断 | — | — | — |
| 2 | 君主 且 目标 owner ≠ 本人物 id | 阻断 | — | — | — |
| 3 | 君主 且 目标 owner == 本人物 id | 改 | 改 | 不变 | 不变 |
| 4 | 非君主 且 目标有主 且 当前 faction == 目标 owner | 改 | 改 | 不变 | 见 §3.7 |
| 5 | 非君主 且 目标有主 且 当前 faction ≠ 目标 owner | 改 | 改 | 改为目标 owner | 见 §3.7 |
| 6 | 非君主 且 目标无主 | 改 | 改 | 改为 None（下野） | 不变 |

- 君主判定：`Character.is_ruler()`（等价于 `faction is not None and faction == id`）。
- 「改」= 字段值改为目标值；「不变」= 该字段不进 `new_values`。
- `role` 字段本次不动。

### 3.7 登场询问

| 项 | 规则 |
|---|---|
| 触发条件 | 选中集含至少 1 个未登场人物 **且** 目标据点有主 |
| 形式 | `messagebox.askyesno`，`default="yes"` |
| 文案要点 | 说明「目标据点有势力，是否同时将选中的未登场人物设为登场」 |
| 选「是」 | 上述未登场人物的 `appeared` 改为 `True` |
| 选「否」 | 上述未登场人物的 `appeared` 保持原值 |
| 无论选是或否 | `node` / `location` / `faction` 均按 §3.6 照常变更 |

### 3.8 二次确认

在弹窗点「确定」后弹出：

| 项 | 内容 |
|---|---|
| 形式 | `messagebox.askyesno` |
| 内容 | 目标据点（州 · 郡 · 县 · 势力）+ 各字段变更人数（node / faction / appeared）+ 被阻断君主（姓名或计数） |
| 确认 | 生成命令并执行 |
| 取消 | 返回弹窗，不关闭 |

### 3.9 命令生成

| 项 | 规则 |
|---|---|
| 单人物 | `CharacterEditCommand(cid, old_values, new_values)` |
| 多人物 | `CompositeCommand(cmds, "移动到据点")` |
| `old_values` / `new_values` | 只含**真正变化**的字段（参照 §10.2 约定） |
| 阻断者 | 不进 `cmds` |
| 目标 = 当前 `node` 者 | 不进 `cmds`（无字段变化） |
| 全部无变化 | 不生成命令，不触发 dirty |

### 3.10 已在目标据点

| 场景 | 处理 |
|---|---|
| 全部选中人 `node` == 目标 | 弹 `messagebox.showinfo`「已在该据点」后返回（不进入二次确认，不生成命令） |
| 部分在 | 只对不在的人生成命令；信息块计数**不含**已在目标据点的人 |

### 3.11 执行后刷新

沿用现有链路：`session.execute` → `_notify_edit` → `SidePanel.on_panel_edit` → `refresh_all` + `on_edit_executed`。

四个面板的聚合（势力人物数 / 据点人物数）自动反映，无需额外处理。

---

## 4. 边界与兼容

| 场景 | 处理 |
|---|---|
| 老剧本无 `appeared` 字段 | 沿用 §3.3，默认 `True`；不触发 §3.7 询问 |
| 未登场人物 `faction` / `node` / `location` 为 `None` | 视为当前值；移动到有主据点后按 §3.6 第 5 条处理 |
| 目标 owner 是脏数据（`world.factions` 无对应） | 视为无主（§3.5） |
| 原数据已存在 `node.owner ≠ faction` 的不一致 | 本次不修，移动到新据点后以目标 owner 为准；原先的不一致在移动后可能被修正，也可能持续（若目标 owner 恰好也 ≠ faction） |
| 选中集混合君主 / 普通人 | 君主按 §3.6 阻断，普通人照常；提示君主被阻断 |
| 选中集全部为君主且全部被阻断 | 不生成命令，弹提示后关闭弹窗 |
| 据点列表为空（无任何据点行） | 弹窗显示「无可用据点」，「确定」按钮置灰 |
| 搜索过滤后 | 列表按过滤结果显示；信息块基于当前选中行实时计算，与过滤无关 |
| 弹窗打开期间剧本被外部修改 | 不支持，弹窗为模态 |
| 连续多次移动 | 每次走一次完整流程；undo / redo 走现有 EditSession 路径 |
| `node` / `location` 的运行时语义 | 本次仅做编制变更；任务系统 / 部队联动判断不在范围内（见 §5、§9） |

---

## 5. 不要做的事

| 编号 | 禁止 |
|---|---|
| N1 | **不**引入「势力是否变更」的用户选择项（faction 自动跟随，不可否决） |
| N2 | **不**在无主据点场景下询问登场状态（§3.7 只在有主时触发） |
| N3 | **不**修改 `Character` 类的字段、默认值或方法语义 |
| N4 | **不**修改 `World.characters_of` / `characters_at` / `count_characters_by_*` 的口径 |
| N5 | **不**修改 `Node` / `Faction` 的模型或派生值逻辑 |
| N6 | **不**修改 `role` 字段 |
| N7 | **不**实现「任务系统」「部队系统」相关的所在判断（见 §9） |
| N8 | **不**在 UI 层直接赋值 `Character.node` / `.location` / `.faction` / `.appeared`，必须走 `CharacterEditCommand`（§8.3 第 36 条） |
| N9 | **不**给右键入口复制模式判断；统一走 `edit=True` + `build_menu`（§8.3 第 40 条） |
| N10 | **不**修改据点面板本身的行为（弹窗内嵌的是裁剪副本，非据点面板实例） |
| N11 | **不**引入新的第三方依赖 |
| N12 | **不**新增第三层弹窗（弹窗 → 确认框最多两层） |

---

## 6. 约束

| 编号 | 约束 |
|---|---|
| C1 | 改 World 只走 Command（§8.3 第 36 条） |
| C2 | 编辑入口统一标识：`MenuItem(..., edit=True)`（§8.3 第 40 条） |
| C3 | `CharacterEditCommand` 的 `old_values` / `new_values` 只含真正变化的字段（§10.2） |
| C4 | 多人操作用 `CompositeCommand`；undo 逆序还原 |
| C5 | 弹窗模态，与 `MainWindow.open_edit_dialog` 的模态机制一致 |
| C6 | 弹窗内据点列表的行为等价于据点面板的：搜索 / 排序 / 列解析 / 滚动 / 行模型 |
| C7 | 术语严格与 README §0 一致：「所属」「所在」「登场」「势力」 |
| C8 | 日志：一次移动记一条 info（含目标据点 id、变更人数、阻断人数、跳过人数） |
| C9 | 改动只新增（新弹窗、新右键项、可能的新列表子类）；对现有面板与框架文件只做必要扩展，不重构 |

---

## 7. 验收清单

### 7.1 入口

- [ ] 人物面板右键菜单出现「移动到据点」，位置在其他编辑入口附近。
- [ ] 非编辑模式下该项置灰。
- [ ] 单选一行右键 → 弹窗内选中人数 = 1。
- [ ] 多选后右键 → 弹窗内选中人数 = 选中集人数。

### 7.2 弹窗

- [ ] 弹窗标题为「移动到据点」，模态，相对主窗口居中。
- [ ] 上部据点列表：只能选中一行；Ctrl+A 无效；右键不弹菜单；无分组条。
- [ ] 搜索框可用；列头点击可排序；滚动条可用。
- [ ] 未选中任何据点时，信息块显示「请选择一个据点」，「确定」置灰。
- [ ] 选中一个据点后，信息块立即刷新。

### 7.3 信息块

- [ ] 显示目标据点：州 · 郡 · 县 · 类型 · 规模 · 势力（无主显示「无主」）。
- [ ] 显示选中人数。
- [ ] 显示「会改变据点」人数。
- [ ] 显示「仅换据点、势力不变」人数，独立成行。
- [ ] 显示「会变更势力」人数。
- [ ] 显示「会下野」人数。
- [ ] 显示「会设为登场」人数，并标注将询问。
- [ ] 显示被阻断君主姓名或计数。

### 7.4 变更正确性

- [ ] 非君主移动到有主据点：node、location、faction 均改为目标值。
- [ ] 非君主移动到无主据点：node、location 改；faction 改为 None。
- [ ] 非君主移动到当前 faction 自己的据点：faction 不变。
- [ ] 君主移动到本势力据点：允许。
- [ ] 君主移动到无主据点：阻断，弹警告，其他普通人照常处理。
- [ ] 君主移动到其他势力据点：阻断，同上。
- [ ] 全部选中都是被阻断的君主：无命令生成，弹提示后关闭弹窗。

### 7.5 登场询问

- [ ] 选中集含未登场人物 + 目标有主：弹出询问，默认「是」。
- [ ] 选「是」：未登场人物的 appeared 改为 True。
- [ ] 选「否」：appeared 保持原值，node / location / faction 仍按 §3.6 变更。
- [ ] 选中集无未登场人物：不弹询问。
- [ ] 目标无主：不弹询问（无论是否有未登场人物）。

### 7.6 二次确认

- [ ] 弹窗点「确定」后弹出确认框，展示目标据点、变更人数、被阻断君主。
- [ ] 确认 → 命令执行；取消 → 返回弹窗。

### 7.7 执行与刷新

- [ ] 执行后人物面板立即刷新：node / 势力 / 所在列更新；登场列若受影响同步更新。
- [ ] 据点面板「人物」列同步更新。
- [ ] 势力面板「人物」列同步更新。
- [ ] 窗口标题出现 `*`（有未保存改动）。
- [ ] Ctrl+Z 撤销：所有字段回到操作前；标题 `*` 状态正确。
- [ ] Ctrl+Shift+Z 重做：与执行后一致。

### 7.8 边界

- [ ] 全部选中人已在目标据点：弹「已在该据点」提示，不生成命令，不进入二次确认。
- [ ] 部分选中人已在目标据点：只对不在的人生成命令；信息块计数不含已在的人。
- [ ] 搜索过滤后选中一行并移动：只影响该行，不受过滤影响。

---

## 8. README 更新

实现本需求时，README 需同步更新以下内容：

| 位置 | 更新 |
|---|---|
| §5.23 character_panel 右键菜单列表 | 新增「移动到据点」 |
| §6.4 主循环交互表 | 新增「人物右键 → 移动到据点」链路 |
| §8.3 已知逻辑限制 | 新增：`Character.location` 随 `node` 一起变更；任务系统 / 部队联动判断未做，本次先跳过 |
| §8.4 下一步计划 | 新增：任务系统 / 部队系统落地后，需重新评估「人物移动时是否改所在」的判断逻辑 |
| §10.2 具体命令 | 若新增专用命令类，补充说明；若复用 CharacterEditCommand，补一句使用场景 |

---

## 9. 完成后请提供什么

下游实现完成后，请报告：

| 项 | 内容 |
|---|---|
| 改动文件 | 新增 / 修改的文件路径 |
| 关键实现点 | 弹窗结构、据点列表裁剪方式、信息块计算入口、君主阻断接入点、登场询问接入点、命令生成方式 |
| README 更新 | 实际改了哪些章节，与 §8 的对应关系 |
| 测试结果 | 新增 / 修改的单元测试与运行结果 |
| 验收情况 | 逐条对照 §7 勾选，标注未通过项与原因 |
| 已知偏差 | 与本文档不一致之处及理由 |
| 未覆盖项 | 无法实现或需后续处理的部分 |